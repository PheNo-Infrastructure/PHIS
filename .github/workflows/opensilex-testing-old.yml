# Deprecated — superseded by opensilex-testing.yml
name: OpenSILEX Testing Suite (Old)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use opensilex-testing.yml instead."
