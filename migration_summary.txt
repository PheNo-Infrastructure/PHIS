HTTPS Migration Summary
What We've Accomplished ✅
1. Updated the Installer Script (tools/opensilex-installer.sh)
Successfully migrated the OpenSILEX installer from HTTP to HTTPS with these changes:

Configuration Changes:

Line 211: baseURI → https:// (was http)
Line 276: publicURI → https:// (was http)
Line 365: CORS allowedOrigins → HTTPS only
Line 410: FEIDE redirectURI → https:// (was http)
Line 607: GraphDB base-URL → https:// (was http)
Two-Stage Nginx Setup:

Stage 1 (Lines 708-752): HTTP-only config for certificate acquisition
Stage 2 (Lines 858-927): Full HTTPS config with SSL certificates
Automatic HTTP → HTTPS redirect
Modern TLS 1.2/1.3 security settings
Certificate Management:

Certbot installed automatically (Line 702)
Let's Encrypt for valid domains (Line 826)
Self-signed fallback for IP addresses (Lines 814-817)
Auto-renewal configured
2. Created Migration Script (tools/migrate-to-https.sh)
Automated script to migrate existing HTTP installations to HTTPS:

DNS verification
FEIDE update confirmation
Certificate acquisition
Configuration backup
OpenSILEX config updates (all HTTP → HTTPS)
Nginx reconfiguration
Service restarts
3. Test Deployment Successful
Deployed test instance at 108.143.82.78
Self-signed certificate working (expected browser warning)
HTTPS active on port 443
HTTP redirecting to HTTPS
What Needs To Be Done 🔧
Production Server Migration (172.211.86.191 → phis.pheno.no)
Prerequisites (Already Complete):
✅ DNS configured (phis.pheno.no → 172.211.86.191)
✅ FEIDE updated (redirect URI: https://phis.pheno.no/app/openid)
Remaining Steps:
Upload Migration Script


# From laptop with correct SSH key
scp C:\Users\sebas\Documents\GitHub\PHIS\tools\migrate-to-https.sh azureuser@172.211.86.191:/home/azureuser/
SSH into Production


ssh azureuser@172.211.86.191
Run Migration Script


chmod +x migrate-to-https.sh
./migrate-to-https.sh
The script will:

Install Certbot
Obtain Let's Encrypt certificate
Backup existing configs
Update OpenSILEX config (HTTP → HTTPS)
Update Nginx config (add HTTPS + redirect)
Restart services
Verify Migration


# Test HTTPS
curl -I https://phis.pheno.no/

# Check certificate
sudo certbot certificates

# Monitor logs
sudo journalctl -u opensilex -f
Test in Browser

Access https://phis.pheno.no/
Verify valid SSL certificate (no warnings)
Test FEIDE login
Confirm all functionality works
Key Files Modified
File	Purpose	Status
tools/opensilex-installer.sh	Main installer with HTTPS support	✅ Complete
tools/migrate-to-https.sh	Migration script for existing deployments	✅ Ready to run
Rollback Plan (if needed)
Migration script creates automatic backups:


# View backups
ls -la /home/azureuser/opensilex/config/*.backup-*
ls -la /etc/nginx/sites-available/*.backup-*

# Restore
sudo cp /home/azureuser/opensilex/config/opensilex.yml.backup-[timestamp] /home/azureuser/opensilex/config/opensilex.yml
sudo cp /etc/nginx/sites-available/opensilex.backup-[timestamp] /etc/nginx/sites-available/opensilex
sudo systemctl reload nginx
sudo systemctl restart opensilex