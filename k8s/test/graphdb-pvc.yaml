apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: graphdb-data
  namespace: __NS__
spec:
  storageClassName: managed-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
