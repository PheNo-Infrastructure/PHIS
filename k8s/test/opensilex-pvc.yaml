apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opensilex-data
  namespace: __NS__
spec:
  storageClassName: managed-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
