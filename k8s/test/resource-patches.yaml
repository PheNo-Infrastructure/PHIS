apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphdb
spec:
  template:
    spec:
      containers:
        - name: graphdb
          resources:
            requests:
              cpu: "100m"
              memory: "512Mi"
            limits:
              memory: "2Gi"
          env:
            - name: GDB_JAVA_OPTS
              value: >-
                -Xmx1g -Xms256m
                -Dgraphdb.home=/opt/graphdb/home
                -Dgraphdb.workbench.cors.enable=false
        - name: graphdb-proxy
          resources:
            requests:
              cpu: "10m"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: opensilex
spec:
  template:
    spec:
      containers:
        - name: opensilex
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              memory: "1Gi"
          env:
            - name: JAVA_OPTS
              value: "-Xmx512m -Xms256m"
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb
spec:
  template:
    spec:
      containers:
        - name: mongodb
          resources:
            requests:
              cpu: "100m"
