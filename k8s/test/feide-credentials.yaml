apiVersion: v1
kind: Secret
metadata:
  name: feide-credentials
  namespace: phis-test
type: Opaque
stringData:
  client-id: REPLACE_WITH_FEIDE_CLIENT_ID
  client-secret: REPLACE_WITH_FEIDE_CLIENT_SECRET
