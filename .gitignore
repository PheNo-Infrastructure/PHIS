*.pyc
/1.4.8-rdg
/tools/config

# Deployment logs and outputs
deploy-output.log
*.log

# Temporary files
nul
**/nul

# SSH keys and configs (if accidentally copied to repo)
*.pem
*.key
id_rsa*
id_ed25519*
.ssh/
k8s/graphdb/secret.yaml
k8s/mongodb/secret.yaml

# Terraform — never commit real variable values or state
terraform/terraform.tfvars
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/.terraform.tfstate.lock.info
# terraform/.terraform.lock.hcl is intentionally NOT ignored — commit it to pin provider versions

# Scratch artifacts from opensilex debug sessions
dbg-patch*.json
